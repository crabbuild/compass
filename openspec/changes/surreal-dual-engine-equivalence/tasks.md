## 1. Contract and Current-Engine Route

- [x] 1.1 Add the engine-neutral structural-read contract and current-engine bounded subgraph route; verify integration/contract coverage proves exact identities, ambiguity, negative cases, independent limits, canonical ordering, and response-byte enforcement

## 2. Surreal Native Reads

- [x] 2.1 Implement generation-pinned static node/relation statements, typed cursor validation, exact symbol context, impact, directed path, and connected-subgraph operations; verify Mem integration tests cover query-shaped identities, pointer changes, missing generations, parallel/reverse/self-loop relations, provenance, confidence, truncation, and page concatenation

## 3. Dual-Engine and Budget Qualification

- [x] 3.1 Add deterministic semantic-corpus plus medium/large scale-sample differential qualification and require zero semantic mismatches; verify the retained oracle covers every C-013 dimension and both persistent feature profiles compile
- [ ] 3.2 Measure every applicable pre-ratified C-013 gate on the same runner, retain raw identities/samples/ratios/decisions, and invoke the falsifier protocol instead of weakening any failed threshold; verify the qualification manifest closes over every retained artifact

## 4. Documentation and Completion Gates

- [ ] 4.1 Update integration/qualification documentation and changelog; run focused tests, default dependency/binary isolation, all feature checks, workspace formatting/lint/tests, product boundary, strict OpenSpec, graph refresh, refinement, independent adversarial review, verification, spec sync, and archive
