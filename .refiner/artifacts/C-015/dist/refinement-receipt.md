# C-015 refinement receipt

Status: **REJECT**
Date: 2026-08-29

The implementation is bounded and semantically equivalent, and its functional
integration suites pass. The pre-ratified query-regression and native-value
budgets do not pass. This receipt intentionally does not certify adoption,
archive eligibility, a CLI/MCP route, or completion of the rejected OpenSpec
change. Exact evidence is retained in
`benchmarks/qualification/surreal-dual-engine-decision-v1.json`.

The installed artifact-refiner adapter lacks its declared canonical controllers
and schemas, so this is the deterministic repository fallback receipt.
